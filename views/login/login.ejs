<%- include('../partials/head.ejs', {pageTitle: pageTitle}) %>
<link rel="stylesheet" href="/css/login.css">
<link rel="stylesheet" href="/css/form.css">

  </head>

  <body class="box">
    <%- include('../partials/header.ejs') %>
    <div class="login-box fadein">
      <p class="login-title font-grey-light">WELCOME TO INFINITY</p>
      <form class="login-box" action="/login" method="post">
        <input type="hidden" name="_csrf" value="<%= csrfToken %>">
        <input
          id="email"
          name="email"
          class="form-input <%= validationErrors.find(e => e.path === 'email') ? 'invalid' : '' %>"
          type="text"
          placeholder="Email"
          value="<%= oldInput.email %>"
          required
        />
        <input
          id="password"
          name="password"
          class="form-input <%= validationErrors.find(e => e.path === 'password') ? 'invalid' : '' %>"
          type="password"
          placeholder="Password"
          value="<%= oldInput.password %>"
          required
        />
        <% if (errorMessage.length > 0) { %>
        <p class="form__error-text"><%= errorMessage %></p>
        <% } %>
        <button type="submit" class="form-btn">LogIn</button>
      </form>
      <a href="/reset" class="link__reset-password font-grey-light">Forgot your password?</a>
    </div>
    <%- include('../partials/footer.ejs') %>
    <%- include("../partials/end.ejs") %>
